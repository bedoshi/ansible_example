---
- hosts: all
  gather_facts: false
  tasks:
  - name: Cloning repo
    ansible.builtin.git:
      repo: https://github.com/bedoshi/nginx_react_boot.git
      dest: ~/dev
  - name: Installing node packages
    npm:
      path: "~/dev/frontend"
      state: present
  - name: Build app
    command: npm run build
    args:
      chdir: "~/dev/frontend"
  - name: Deploy fornt-app
    copy:
      src: ~/dev/frontend/build
      dest: /usr/share/nginx/build
  - name: Build serverside app
    command: ./gradlew build
    args:
      chdir: "~/dev/serverside"
